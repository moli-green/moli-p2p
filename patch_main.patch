--- client/src/main.ts
+++ client/src/main.ts
@@ -23,7 +23,7 @@
 import { updateConnectionStatus } from './ui';
 import { P2PNetwork } from './P2PNetwork';
 import { Logger } from './lib/logger'; // Phase X: Universal Logger
-import { PeerSession } from './PeerSession';
+import { PeerSession, FileOffer } from './PeerSession';
 import { blobHashRegistry } from './blobRegistry';
 import { MoliAPI } from './api';
 import { wrapPromise } from './lib/Result';
@@ -298,7 +298,7 @@
       console.log(`[Validation] Testing dimensions for ${file.name}`);
       const img = new Image();
       const objectUrl = URL.createObjectURL(file);
-      const MAX_DIMENSION = 8192; // 8K allowed, anything larger is likely an attack or wallpaper
+      const MAX_DIMENSION = 8192;

       img.onload = () => {
         URL.revokeObjectURL(objectUrl);
@@ -421,8 +421,9 @@
         // Physically remove standard DOM element from DOM
         toRemove.element.remove();
         // Cleanup internal UI event listeners/objectURLs if cleanup method exists
-      if (typeof (toRemove.element as any).cleanup === 'function') {
-        (toRemove.element as any).cleanup();
+      const elWithCleanup = toRemove.element as HTMLElement & { cleanup?: () => void };
+      if (typeof elWithCleanup.cleanup === 'function') {
+        elWithCleanup.cleanup();
       }

         // Find logical position
@@ -441,8 +442,9 @@
       const item = sortedItems.pop()!;
       item.element.remove(); // Remove standard DOM element

-      if (typeof (item.element as any).cleanup === 'function') {
-        (item.element as any).cleanup();
+      const elWithCleanup = item.element as HTMLElement & { cleanup?: () => void };
+      if (typeof elWithCleanup.cleanup === 'function') {
+        elWithCleanup.cleanup();
       }

       const idx = imageStore.findIndex(i => i.hash === item.hash);
@@ -455,7 +457,7 @@

 // --- Auto-Download Queue ---
 let isDownloading = false;
-const downloadQueue: { session: PeerSession; transferId: string; meta: any }[] = [];
+const downloadQueue: { session: PeerSession; transferId: string; meta: FileOffer }[] = [];

 async function processDownloadQueue() {
   if (isDownloading || downloadQueue.length === 0) return;
@@ -483,7 +485,7 @@
   // Promise wrappers for UI callbacks (Phase 46)
   let uiResolve: () => void;
-  let uiReject: (reason?: any) => void;
+  let uiReject: (reason?: unknown) => void;
   const uiPromise = new Promise<void>((resolve, reject) => {
     uiResolve = resolve;
@@ -838,7 +840,7 @@
       return;
     }
   },
-  (session: PeerSession, data: any) => { // Offer File Callback
+  (session: PeerSession, data: FileOffer) => { // Offer File Callback
     // Only queue if we want auto-download
     if (isAutoDownloadEnabled) {
       downloadQueue.push({ session, transferId: data.transferId, meta: data });
@@ -1082,7 +1084,7 @@
       await p2pNetwork.start();
       Logger.info('Signaling System initialized via UI');
       connectionBtn.disabled = false;
-  } catch (err: any) {
+  } catch (err: unknown) {
       Logger.error('Failed to connect to signaling server:', err);
       alert(`Failed to connect to signaling server. Refresh to try again.\n\nError: ${err instanceof Error ? err.message : String(err)}`);
   }
@@ -1136,7 +1138,8 @@
   h2.style.fontSize = '3rem';
   h2.style.background = 'linear-gradient(45deg, #FF6B6B, #4ECDC4)';
   h2.style.webkitBackgroundClip = 'text';
-  (h2.style as any).webkitTextFillColor = 'transparent';
+  // @ts-ignore
+  h2.style.webkitTextFillColor = 'transparent';
   h2.style.marginBottom = '20px';

   const p = document.createElement('p');
